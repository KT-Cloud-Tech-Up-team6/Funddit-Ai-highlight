# 서버 운영에 필요한 것만. PoC 비교 도구는 requirements-dev.txt 에 있다.

google-genai>=1.0

# 음성 인식 (로컬) — GPU 는 nvidia-* 휠이 CUDA 런타임을 제공한다 (툴킷 설치 불필요)
faster-whisper>=1.2
nvidia-cublas-cu12
nvidia-cudnn-cu12

# API 서버
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0

# 영상 분석·썸네일
pillow>=10.0
numpy>=1.26
